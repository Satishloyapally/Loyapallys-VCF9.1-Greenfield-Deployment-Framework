# Copy to secrets.auto.tfvars (git-ignored) and fill in real values.
# These credentials belong to the WORKLOAD DOMAIN vCenter and NSX Manager
# (the same values supplied to stage 40).

vsphere_password = "CHANGE-ME" # administrator@vsphere.local on the WLD vCenter
nsx_password     = "CHANGE-ME" # admin on the WLD NSX Manager
