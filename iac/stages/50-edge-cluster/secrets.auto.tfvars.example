# Copy to secrets.auto.tfvars (git-ignored) and fill in real values.
# One credentials entry per edge cluster declared in site.yaml.
# Edge passwords require 15+ characters.

sddc_manager_password = "CHANGE-ME" # administrator@vsphere.local

credentials = {
  "dc01-wld-01-ec01" = {
    root_password  = "CHANGE-ME"
    admin_password = "CHANGE-ME"
    audit_password = "CHANGE-ME"
    # bgp_password = "CHANGE-ME"  # optional MD5 auth towards the fabric
  }
}
