# Copy to secrets.auto.tfvars (git-ignored) and fill in real values.
# One credentials entry per workload domain declared in site.yaml.

sddc_manager_password = "CHANGE-ME" # administrator@vsphere.local

credentials = {
  "dc01-wld-01" = {
    vcenter_root_password = "CHANGE-ME"
    nsx_admin_password    = "CHANGE-ME"
    nsx_audit_password    = "CHANGE-ME"
    # sso_admin_password  = "CHANGE-ME"  # only for isolated SSO domains
  }
}
